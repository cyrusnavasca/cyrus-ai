# Training deps. CUDA GPU required (Colab / rented GPU). Not installable on macOS.
# Install unsloth first; it pins compatible torch/transformers builds.
unsloth
unsloth_zoo
transformers>=4.44
peft>=0.12
trl>=0.9
datasets>=2.20
bitsandbytes>=0.43
accelerate>=0.33
torch>=2.3
